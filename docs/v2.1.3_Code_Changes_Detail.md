# SnapTXT v2.1.3 코드 변경 상세 기록

**작성일**: 2026년 3월 6일  
**목적**: v2.1.3 실제 변경사항 구체적 기록  

---

## 🔧 **실제 수정된 파일별 상세 변경사항**

### **1. Import 계층 정리 상세**

#### **파일**: `snaptxt/postprocess/patterns/stage_pipeline_processor.py`

**변경 전**:
```python
def _run_stage2_basic(text: str) -> str:
    """기존 Stage2 처리 (호환성)"""
    from snaptxt.backend.worker.easyocr_worker import _STAGE2_LOGGER  # 순환 위험
    cfg = Stage2Config(logger=_STAGE2_LOGGER)
    return apply_stage2_rules(text, cfg)

def _run_stage3_basic(text: str) -> str:
    """기존 Stage3 처리 (호환성)"""
    from snaptxt.backend.worker.easyocr_worker import _STAGE3_LOGGER  # 순환 위험
    cfg = Stage3Config(
        # ... 설정들
        logger=_STAGE3_LOGGER,
    )
    return apply_stage3_rules(text, cfg)
```

**변경 후**:
```python
def _run_stage2_basic(text: str) -> str:
    """기존 Stage2 처리 (호환성)"""
    import logging
    stage2_logger = logging.getLogger('snaptxt.stage2')
    cfg = Stage2Config(logger=stage2_logger)
    return apply_stage2_rules(text, cfg)

def _run_stage3_basic(text: str) -> str:
    """기존 Stage3 처리 (호환성)"""
    import logging
    stage3_logger = logging.getLogger('snaptxt.stage3')
    cfg = Stage3Config(
        # ... 설정들
        logger=stage3_logger,
    )
    return apply_stage3_rules(text, cfg)
```

**변경 라인**: 47-48라인, 53-54라인

#### **파일**: `snaptxt/postprocess/patterns/runtime_observer.py`

**변경 전**:
```python
    except ImportError:
        # 폴백: 기존 프로세서
        from snaptxt.backend.worker.easyocr_worker import advanced_korean_text_processor
        logger.warning("📊 메타데이터 프로세서 사용불가, 기존 방식으로 폴백")
        return advanced_korean_text_processor
```

**변경 후**:
```python
    except ImportError:
        # 폴백: 기존 프로세서 대신 에러 처리
        logger.error("📊 메타데이터 프로세서 사용불가, stage_pipeline_processor 모듈 확인 필요")
        raise ImportError("패턴 프로세서를 찾을 수 없습니다")
```

**변경 라인**: 67-70라인

---

### **2. Scope Guard 문자열 상수화 상세**

#### **파일**: `snaptxt/postprocess/patterns/stage_scope_guard.py`

**변경 전**:
```python
"""Stage2/Stage3 현상 기반 스코프 가드 - 패턴별 중복 방지"""
import re
import hashlib
from collections import Counter
from typing import Set, Optional
from dataclasses import dataclass, field

@dataclass
class StageCounter:
```

**변경 후**:
```python
"""Stage2/Stage3 현상 기반 스코프 가드 - 패턴별 중복 방지"""
import re
import hashlib
from collections import Counter
from typing import Set, Optional
from dataclasses import dataclass, field

# Stage 식별자 상수 (오타 방지)
STAGE2 = "S2"
STAGE3 = "S3"

@dataclass
class StageCounter:
```

**추가 라인**: 8-10라인

**추가 변경 위치**:
- 64-67라인: `get_scope_policy()` 함수의 `"S2"` → `STAGE2`, `"S3"` → `STAGE3`
- 82-84라인: `should_apply_rule()` 함수의 문자열 비교를 상수로 변경
- 101라인: `mark_rule_applied()` 함수의 문자열 비교를 상수로 변경

---

### **3. 스모크 테스트용 규칙 추가**

#### **파일**: `learning_data/stage2_overlay_SMOKE.yaml` (신규 생성)

**내용**:
```yaml
replacements:
  "테스트입 니다": "테스트입니다"
  "입 니다": "입니다"
  "했습 니다": "했습니다"
```

**목적**: v2.1.3 실제 교정 기능 작동 증명용 최소 규칙셋

---

### **4. 스모크 테스트 스크립트**

#### **파일**: `test_v2_1_3_smoke.py` (신규 생성)

**핵심 검증 로직**:
```python
def test_stage2_basic_processing():
    """Stage2 기본 처리 테스트"""
    test_text = "테스트입 니다"
    expected = "테스트입니다"
    
    from snaptxt.postprocess.patterns.stage_pipeline_processor import _run_stage2_basic
    
    result = _run_stage2_basic(test_text)
    
    if expected in result or "테스트입니다" in result:
        return True  # 성공
```

**결과**: 3/3 테스트 통과 (100%)

---

## 📊 **변경사항 요약**

| 구분 | 변경 파일 수 | 추가 라인 | 수정 라인 | 삭제 라인 |
|------|-------------|----------|----------|----------|
| Import 정리 | 2개 | 4라인 | 6라인 | 4라인 |
| Scope Guard | 1개 | 3라인 | 6라인 | 0라인 |
| 스모크 테스트 | 2개 | 150라인 | 0라인 | 0라인 |
| **총계** | **5개** | **157라인** | **12라인** | **4라인** |

---

## ✅ **검증 완료 사항**

1. **순환 import 제거**: grep 검색으로 backend import 완전 제거 확인
2. **상수화 완료**: "S2", "S3" 하드코딩 모두 STAGE2, STAGE3 상수로 교체 
3. **실제 동작**: "테스트입 니다" → "테스트입니다" 변환 실제 작동 확인
4. **호환성 유지**: 기존 API 변경 없음, 내부 구현만 안전화

---

## 🔒 **안전성 확보**

- **Import 안전성**: patterns → backend 의존성 완전 차단
- **Runtime 안전성**: 문자열 오타로 인한 로직 실패 위험 제거  
- **테스트 안전성**: 실제 작동 증명으로 기능 회귀 방지

*이 문서는 v2.1.3의 모든 코드 변경사항을 정확히 기록합니다.*

---

**Last verified on**: 2026-03-06  
**Verified by**: 실제 파일 수정 및 스모크 테스트  
**Evidence**: "테스트입 니다" → "테스트입니다" 변환 작동